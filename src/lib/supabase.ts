// This file is no longer needed - all functionality moved to Firebase
// Keeping empty file to avoid import errors during transition

export interface Reservation {
  // Legacy interface - use Firebase types instead
}